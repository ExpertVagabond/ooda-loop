[project]
name = "ooda-loop"
version = "0.1.0"
description = "Autonomous OODA coding loop — local inference + HVE constraints + Grok review"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[project.scripts]
ooda = "ooda:main"
